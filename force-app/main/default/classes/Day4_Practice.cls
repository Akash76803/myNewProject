public class Day4_Practice {

    public static void getContact(){
        map<id, list<Account>> acc = new map<id, list<Account>>();
        
        for(Contact c: [select id, name, Primary_contact__c from contact where Primary_contact__c = true]){
            
        }
    }
}